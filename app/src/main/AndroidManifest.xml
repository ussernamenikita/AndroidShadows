<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.composeshadows">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeShadows">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeShadows.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.screen.GradientShadowActivity" />
        <activity android:name=".ui.screen.GradientShadowCustomizationActivity" />
        <activity android:name=".ui.screen.ElevationShadowActivity" />
        <activity android:name=".ui.screen.MaterialShapeDrawableActivity" />
        <activity android:name=".ui.screen.ScriptIntrinsicBlurActivity" />
        <activity android:name=".ui.screen.ShadowLayerActivity" />
    </application>

</manifest>